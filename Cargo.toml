[package]
name = "mixing-sort"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.0"
rayon = "1"
plotters = { version = "0.3", default-features = false, features = ["bitmap_backend", "line_series", "bitmap_encoder", "ttf"] }
